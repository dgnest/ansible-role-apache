---
# vars file for hadenlabs.apache

_apache:
  path:
    sites-available: /etc/apache2/sites-available

# _apache_create_files:
#   - path: "{{ _path_dir }}"
#     state: "directory"
#     # group: "root"
#     owner: "{{ user }}"

_apache_domain_conf: "{{ _apache.path.sites-available }}/{{apache_domain}}".vhost.conf

_apache:
  templates:
    - src: "vhost.conf.j2"
      dest: {{ _apache_domain_conf }}
